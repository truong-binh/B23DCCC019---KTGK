import React from 'react';
import { Card } from 'antd';

const CreateExamPage: React.FC = () => {
	return <Card title='Tạo đề thi mới'>{/* Nội dung trang tạo đề thi */}</Card>;
};

export default CreateExamPage;
